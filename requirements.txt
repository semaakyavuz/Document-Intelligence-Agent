# generate_invoices.py
Faker
Pillow

# augment_invoices.py
augraphy
albumentations
opencv-python-headless
numpy

# app/ (config, state, providers, agents)
pydantic
pydantic-settings
requests
google-genai  # google-generativeai artik legacy (deprecated); Google'in resmi devam paketi
chromadb  # RAG Agent: yerel, persist edilen vektor veritabani
langgraph  # app/graph.py: dort ajani StateGraph olarak zincirler
mcp  # app/mcp/rules_server.py: RAG kural sorgulamasi icin MCP server/client (resmi Python SDK, FastMCP dahil)
sqlalchemy  # app/db/: InvoiceRecord ORM modeli + engine/session
psycopg[binary]  # PostgreSQL driver (psycopg2 degil: psycopg3 aktif gelistirilen resmi halef)

# app/api/: HTTP katmani
fastapi
uvicorn[standard]
python-multipart  # FastAPI'de UploadFile (multipart form) icin gerekli

# scripts/evaluate_vision_agent.py: esnek tarih parse (format farkindan bagimsiz karsilastirma)
python-dateutil

# testler
pytest

# lint (CI: .github/workflows/ci.yml)
# Surum sabit: yeni ruff surumleri yeni kural ekleyebiliyor ve hicbir sey
# degismeden CI'i aniden kirmiziya cevirebiliyor. Kural seti ruff.toml'da.
ruff==0.16.7
