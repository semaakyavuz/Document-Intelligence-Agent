# Yalnizca UYGULAMANIN CALISMASI icin gerekenler (Dockerfile bu dosyayi kurar).
# Script'ler, veri uretimi ve testler icin gerekenler requirements-dev.txt'te.
#
# Bu liste app/ altindaki importlar AST ile taranarak cikarildi; "lazim olabilir"
# diye eklenmis paket yok.

# app/ (config, state, providers, agents)
pydantic
pydantic-settings
requests  # app/providers/ollama_provider.py
httpx  # app/providers/gemini_provider.py: hata turleri (TimeoutException/TransportError) dogrudan import ediliyor
google-genai  # google-generativeai artik legacy (deprecated); Google'in resmi devam paketi
chromadb  # RAG: yerel, persist edilen vektor veritabani
langgraph  # app/graph.py: dort ajani StateGraph olarak zincirler
mcp  # app/mcp/rules_server.py: RAG kural sorgulamasi icin MCP server/client (resmi Python SDK, FastMCP dahil)
sqlalchemy  # app/db/: InvoiceRecord ORM modeli + engine/session
psycopg[binary]  # PostgreSQL driver (psycopg2 degil: psycopg3 aktif gelistirilen resmi halef)

# >=10.1: ImageFont.load_default() "size" argumanini bu surumde aldi (bkz. requirements-dev
# icindeki generate_invoices notu). app/ tarafinda da runtime: Gemini vision cagrisi
# gorseli PIL ile aciyor (app/providers/gemini_provider.py).
Pillow>=10.1

# app/api/: HTTP katmani
fastapi
uvicorn[standard]
python-multipart  # FastAPI'de UploadFile (multipart form) icin gerekli
